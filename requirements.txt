# movieedit2 ランタイム依存（PyPI 分）。
# torch / torchvision は CUDA 12.8 専用 index から入れるので Dockerfile 側で別途インストール。
# 現行の稼働環境(2026-07)のバージョンに固定して再現性を担保する。

onnxruntime-gpu==1.24.4
insightface==0.7.3
sam2==1.1.0

fastapi==0.135.3
uvicorn==0.44.0
python-multipart==0.0.26
pydantic==2.12.4

# Claude 自動クリック / 曖昧判定（autoclick.py）。
# これが無いとコンテナ内で自動クリックが黙って無効になる（.env にキーがあっても）
anthropic==0.89.0

numpy==2.4.4
opencv-python-headless==4.13.0.92

# 主要な依存（明示ピン。無くても解決されるが再現性のため固定）
hydra-core==1.3.3
omegaconf==2.3.1
iopath==0.1.10
onnx==1.21.0
scikit-image==0.26.0
scipy==1.17.1
pillow==11.3.0
